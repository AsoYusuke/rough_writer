<% flash.each do |color, message| %>
  <div class="alert alert-<%= color %> text-success" role="alert"><%= message %>
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
<% end %>